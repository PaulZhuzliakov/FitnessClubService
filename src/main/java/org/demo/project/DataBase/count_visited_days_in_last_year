-- считает количество посещений за последний год. команда работает, но не понял как передать из неё число не использую в проекте
SELECT COUNT(*) FROM attendance WHERE client_id = 53 and date >= (NOW() - INTERVAL '365 DAYS')